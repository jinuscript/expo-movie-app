import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SearchBar = () => {
  return (
    <View className="flex">
      <Text className="text-white">SearchBarddasdlakmslkmlk</Text>
    </View>
  );
};

export default SearchBar;

const styles = StyleSheet.create({});
